import { Card } from "@mui/material";
import styled from "styled-components";

export const TotalsContainer = styled(Card)`
    display: flex;
    position: absolute;
    bottom: 5%;
    left: 75%;
    flex-direction: row;
    justify-content: space-between;
    gap: 15px;
    padding: 20px 25px 20px 25px;
`;